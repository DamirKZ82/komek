services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: komek
      POSTGRES_PASSWORD: komek
      POSTGRES_DB: komek
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U komek"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
